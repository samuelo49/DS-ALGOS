"""
Be able to create a doubly linked list
- Make sure you master how to insert, delete and traverse in doubly linked list
"""
class DoublyLLNode:
    def __init(self,next=None,prev=None,value=0):
        self.value = value
        self.next = next
        self.prev = prev
